"#!/usr/bin/env bash\n# ==============================================================================\n# CloudMesh Production Deployment Script\n# ==============================================================================\n# This script builds the React dashboard locally and deploys the codebase\n# to the production server via rsync and SSH.\n# ==============================================================================\n\nset -euo pipefail\n\n# Target Server Configuration\nTARGET_IP=\"49.13.65.81\"\nTARGET_USER=\"root\"\nTARGET_DIR=\"/home/cloudmesh\"\nREMOTE_TARGET=\"${TARGET_USER}@${TARGET_IP}:${TARGET_DIR}\"\n\necho \"==================================================\"\necho \"🚀 Deploying CloudMesh to ${TARGET_IP}...\"\necho \"==================================================\"\n\n# Step 1: Build React Frontend Locally\necho \"📦 Building React frontend dashboard locally...\"\ncd frontend\nif ! command -v pnpm &> /dev/null; then\n    echo \"❌ Error: pnpm is required to build the frontend. Install it via 'npm install -g pnpm'.\"\n    exit 1\nfi\n\npnpm install\npnpm build\ncd ..\n\n# Step 2: Synchronize files to remote server via rsync\necho \"🔄 Syncing codebase to remote server...\"\nrsync -avz --delete \\\n  --exclude='node_modules' \\\n  --exclude='venv' \\\n  --exclude='.git' \\\n  --exclude='.env' \\\n  --exclude='reports/snapshots' \\\n  --exclude='reports/whois_cache.json' \\\n  --exclude='reports/data.json' \\\n  --exclude='reports/*.pdf' \\\n  --exclude='frontend/node_modules' \\\n  --exclude='frontend/src' \\\n  --exclude='frontend/public' \\\n  --exclude='frontend/package.json' \\\n  --exclude='frontend/pnpm-lock.yaml' \\\n  --exclude='frontend/tsconfig*' \\\n  --exclude='frontend/vite.config.ts' \\\n  --exclude='frontend/eslint.config.js' \\\n  ./ \"${REMOTE_TARGET}/\"\n\n# Step 3: Run Remote Operations (virtualenv, docker, initial sync)\necho \"⚙️ Running remote environment updates...\"\nssh \"${TARGET_USER}@${TARGET_IP}\" bash -c \"\n  cd ${TARGET_DIR}\n  \n  # 1. Ensure ve
<truncated 1095 bytes>